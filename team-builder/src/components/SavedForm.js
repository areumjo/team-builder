import React from "react";

function SavedForm(props) {
    console.log(props);
    return (
        <div>
            {/* <p>{props.savedMember.name} {props.savedMember.role} {props.savedMember.email}</p> */}
        </div>
    )
}

export default SavedForm;